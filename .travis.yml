language: android
android:
  components:
  - tools
  - platform-tools
  - build-tools-25.0.1
  - android-25
licenses:
    - 'android-sdk-preview-license-.+'
    - 'android-sdk-license-.+'
    - 'google-gdk-license-.+'
jdk:
  - oraclejdk8

sudo: false

script: 'travis_retry ./gradlew build'